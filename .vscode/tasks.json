{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "uv: sync dependencies",
            "type": "shell",
            // The command is simply 'uv sync'
            "command": "uv sync",
            "group": "build",
            // Use the standard PowerShell shell for the task
            "options": {
                "shell": {
                    "executable": "powershell.exe",
                    "args": ["-c"] // This tells PowerShell to execute the command string
                }
            },
            "presentation": {
                "reveal": "always", // Set to 'always' for debugging this issue
                "panel": "dedicated"
            }
        }
    ]
}